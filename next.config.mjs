/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'elyouywfluuedeezeydq.supabase.co',
      },
    ],
  },
};

export default nextConfig;
